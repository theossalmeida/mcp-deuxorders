{
  "name": "deuxorders-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.20.0"
  },
  "bin": {
    "deuxorders-mcp": "./dist/bin.js"
  },
  "files": [
    "dist",
    "invokta.mcp.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json --pretty false",
    "typecheck": "tsc -p tsconfig.json --pretty false --noEmit && tsc -p tsconfig.test.json --pretty false --noEmit",
    "test": "vitest run",
    "check": "tsc -p tsconfig.json --pretty false --noEmit && tsc -p tsconfig.test.json --pretty false --noEmit && vitest run && tsc -p tsconfig.json --pretty false && npm run check:mcp",
    "check:mcp": "BACKEND_URL=http://localhost:5047 BACKEND_SERVICE_EMAIL=check@local BACKEND_SERVICE_PASSWORD=placeholder MCP_AUTH_TOKEN=conformance-check-placeholder-token-value invokta check-mcp ./dist/engine.js",
    "direct": "node dist/direct.js",
    "devtools": "BACKEND_URL=http://localhost:5047 BACKEND_SERVICE_EMAIL=check@local BACKEND_SERVICE_PASSWORD=placeholder MCP_AUTH_TOKEN=conformance-check-placeholder-token-value tsc -p tsconfig.json --pretty false && invokta-devtools serve dist/engine.js --watch --build \"tsc -p tsconfig.json --pretty false\"",
    "devtools:doctor": "BACKEND_URL=http://localhost:5047 BACKEND_SERVICE_EMAIL=check@local BACKEND_SERVICE_PASSWORD=placeholder MCP_AUTH_TOKEN=conformance-check-placeholder-token-value tsc -p tsconfig.json --pretty false && invokta-devtools doctor dist/engine.js",
    "cli": "node dist/cli.js",
    "mcp:stdio": "node dist/mcp-stdio.js",
    "mcp:install": "tsc -p tsconfig.json --pretty false && invokta-installer install --engine .",
    "mcp:uninstall": "invokta-installer remove --engine .",
    "mcp:http": "node dist/mcp-http.js",
    "deploy:package": "invokta-deploy package",
    "deploy:probe": "invokta-deploy probe",
    "smoke": "tsc -p tsconfig.json --pretty false && node scripts/smoke.mjs",
    "healthcheck": "node deploy/healthcheck.mjs",
    "provision": "tsc -p tsconfig.json --pretty false && node scripts/provision-service-user.mjs"
  },
  "dependencies": {
    "@invokta/cli": "0.7.0",
    "@invokta/core": "0.7.0",
    "@invokta/installer": "0.7.0",
    "@invokta/mcp": "0.7.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@invokta/deploy": "0.7.0",
    "@invokta/devtools": "0.7.0",
    "@invokta/tooling": "0.7.0",
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vitest": "^4.1.10"
  },
  "description": "Camada MCP do DeuxOrders: capabilities de dominio sobre o DeuxOrders Backend."
}
